---
import '../styles/global.scss'
import logo from '../assets/logo.png'
---

<html lang="fr" class="theme-contrasted">
  <head>
    <meta charset="utf-8" />
    <title>MT-CONGÉS | Paul Blanc</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" href="/icons/favicon-48x48.png" />
    <style lang="scss">
      body {
        margin: 0;
        background-color: #0b0b0b;
        color: #e0e0e0;
        font-family: 'Inter', sans-serif;
        overflow-x: hidden;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
      }

      header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        padding: 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        z-index: 100;
        mix-blend-mode: difference;

        a {
          text-decoration: none;
          color: white;
          font-weight: bold;
          text-transform: uppercase;
          letter-spacing: 0.1em;
        }
      }

      .hero {
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        position: relative;
        overflow: hidden;

        &__bg {
          position: absolute;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          background: radial-gradient(
            circle at 50% 50%,
            #2a2a2a 0%,
            #0b0b0b 70%
          );
          z-index: -1;
          opacity: 0.5;
        }

        h1 {
          font-size: clamp(3rem, 10vw, 8rem);
          line-height: 0.9;
          margin: 0;
          text-transform: uppercase;
          font-weight: 800;
          letter-spacing: -0.02em;
          background: linear-gradient(to right, #fff, #888);
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          opacity: 0;
          transform: translateY(30px);
          animation: fadeUp 1s ease-out forwards 0.5s;
        }

        .subtitle {
          font-size: clamp(1rem, 2vw, 1.5rem);
          margin-top: 2rem;
          color: #888;
          max-width: 600px;
          opacity: 0;
          transform: translateY(20px);
          animation: fadeUp 1s ease-out forwards 0.8s;
        }
      }

      .section {
        padding: 8rem 0;
        border-top: 1px solid rgba(255, 255, 255, 0.1);

        h2 {
          font-size: 2.5rem;
          margin-bottom: 3rem;
          color: #fff;
          text-transform: uppercase;
        }
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 3rem;

        .card {
          background: rgba(255, 255, 255, 0.03);
          border: 1px solid rgba(255, 255, 255, 0.05);
          padding: 2rem;
          border-radius: 8px;
          transition:
            transform 0.3s ease,
            background 0.3s ease;

          &:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.05);
          }

          h3 {
            font-size: 1.25rem;
            margin-top: 0;
            margin-bottom: 1rem;
            color: #fff;
          }

          p {
            color: #aaa;
            line-height: 1.6;
            font-size: 0.95rem;
          }
        }
      }

      .tech-stack {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;

        span {
          padding: 0.5rem 1rem;
          border: 1px solid rgba(255, 255, 255, 0.2);
          border-radius: 50px;
          font-size: 0.9rem;
          color: #ccc;
          transition: all 0.3s ease;

          &:hover {
            border-color: #fff;
            color: #fff;
          }
        }
      }

      .back-btn {
        display: inline-block;
        margin-top: 2rem;
        padding: 1rem 2rem;
        background: #fff;
        color: #000;
        text-decoration: none;
        font-weight: bold;
        border-radius: 4px;
        transition: transform 0.2s;

        &:hover {
          transform: scale(1.05);
        }
      }

      footer {
        padding: 4rem 0;
        text-align: center;
        color: #555;
        font-size: 0.9rem;
      }

      @keyframes fadeUp {
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* Image styling if needed */
      .project-image {
        width: 100%;
        height: auto;
        border-radius: 8px;
        margin-bottom: 4rem;
        opacity: 0;
        animation: fadeUp 1s ease-out forwards 1.2s;
        border: 1px solid rgba(255, 255, 255, 0.1);
      }

      #bg-3d {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
      }
    </style>
  </head>
  <body>
    <canvas id="bg-3d"></canvas>

    <header>
      <a href="/portfolio_bpa/">Paul Blanc</a>
      <a href="https://github.com/Paul-B-06/mt-conges" target="_blank">
        GitHub
      </a>
    </header>

    <main>
      <div class="container">
        <section class="hero">
          <h1>MT-Congés</h1>
          <div class="subtitle">
            Platforme de gestion des temps et congés avec architecture
            hiérarchisée et sécurisée.
          </div>
        </section>

        <img
          src="/portfolio_bpa/images/works/mt-conges-dashboard.png"
          alt="Dashboard"
          class="project-image"
        />

        <section class="section">
          <h2>Le Projet</h2>
          <div class="grid">
            <div>
              <p>
                MT-CONGÉS est une application de gestion des ressources humaines
                conçue pour simplifier et sécuriser le processus de demande et
                de validation des congés. Développée en Java avec une
                architecture stricte MVC, elle répond aux besoins des
                entreprises modernes par sa robustesse et sa hiérarchie des
                rôles.
              </p>
              <p>
                Le projet a été réalisé sur une période de 3 mois dans un cadre
                académique, mettant l'accent sur les bonnes pratiques de
                développement, la sécurité des données et l'expérience
                utilisateur.
              </p>
            </div>
            <div>
              <h3>Objectifs Clés</h3>
              <ul style="color: #aaa; line-height: 1.8;">
                <li>Digitaliser les demandes de congés papiers.</li>
                <li>Assurer une traçabilité complète des actions.</li>
                <li>Sécuriser l'accès aux données sensibles.</li>
                <li>Offrir une interface fluide et intuitive.</li>
              </ul>
            </div>
          </div>
        </section>

        <section class="section">
          <h2>Stack Technique</h2>
          <div class="tech-stack">
            <span>Java 17</span>
            <span>Swing UI</span>
            <span>MySQL</span>
            <span>MVC Pattern</span>
            <span>DAO Pattern</span>
            <span>Git / GitHub</span>
            <span>BCrypt (Sécurité)</span>
          </div>
        </section>

        <img
          src="/portfolio_bpa/images/works/mt-conges-login-screen.png"
          alt="Ecran de Connexion"
          class="project-image"
        />

        <section class="section">
          <h2>Fonctionnalités</h2>
          <div class="grid">
            <div class="card">
              <h3>Hiérarchie des Rôles</h3>
              <p>
                Système complet de gestion des droits : Employés pour les
                demandes, Managers pour la validation d'équipe, et
                Administrateurs pour la configuration globale.
              </p>
            </div>
            <div class="card">
              <h3>Sécurité Avancée</h3>
              <p>
                Protection contre les injections SQL via DAO, hachage des mots
                de passe, et sessions sécurisées pour prévenir les accès non
                autorisés.
              </p>
            </div>
            <div class="card">
              <h3>Logging & Audit</h3>
              <p>
                Traçabilité de toutes les actions critiques (connexions,
                validations, suppressions) pour assurer la conformité et le
                débogage.
              </p>
            </div>
            <div class="card">
              <h3>Interface Responsive</h3>
              <p>
                Interface utilisateur développée en Java Swing avec un design
                moderne et intuitif, facilitant l'adoption par les utilisateurs
                finaux.
              </p>
            </div>
          </div>
        </section>

        <section class="section" style="text-align: center;">
          <h2>Prêt à en voir plus ?</h2>
          <p style="color: #aaa; max-width: 600px; margin: 0 auto 2rem auto;">
            Explorez le code source complet sur GitHub pour découvrir
            l'architecture et les détails d'implémentation.
          </p>
          <a
            href="https://github.com/Paul-B-06/mt-conges"
            target="_blank"
            class="back-btn"
          >
            Voir sur GitHub
          </a>
          <br /><br />
          <a
            href="/portfolio_bpa/"
            style="color: #888; text-decoration: none; font-size: 0.9rem;"
          >
            Retour au Portfolio
          </a>
        </section>
      </div>
    </main>

    <footer>© 2024 Paul Blanc. Tous droits réservés.</footer>

    <script>
      const canvas = document.getElementById('bg-3d')
      const ctx = canvas.getContext('2d')

      let width = (canvas.width = window.innerWidth)
      let height = (canvas.height = window.innerHeight)

      window.addEventListener('resize', () => {
        width = canvas.width = window.innerWidth
        height = canvas.height = window.innerHeight
      })

      const stars = []
      const numStars = 800
      const centerX = width / 2
      const centerY = height / 2

      for (let i = 0; i < numStars; i++) {
        stars.push({
          x: Math.random() * width - centerX,
          y: Math.random() * height - centerY,
          z: Math.random() * width,
        })
      }

      function animate() {
        ctx.fillStyle = '#0b0b0b'
        ctx.fillRect(0, 0, width, height)

        for (let i = 0; i < numStars; i++) {
          const star = stars[i]
          star.z -= 2 // Speed

          if (star.z <= 0) {
            star.x = Math.random() * width - centerX
            star.y = Math.random() * height - centerY
            star.z = width
          }

          const x = (star.x / star.z) * width + centerX
          const y = (star.y / star.z) * height + centerY
          const size = (1 - star.z / width) * 3

          if (x >= 0 && x < width && y >= 0 && y < height) {
            const alpha = 1 - star.z / width
            ctx.fillStyle = 'rgba(255, 255, 255, ' + alpha + ')'
            ctx.beginPath()
            ctx.arc(x, y, size, 0, Math.PI * 2)
            ctx.fill()
          }
        }

        requestAnimationFrame(animate)
      }

      animate()
    </script>
  </body>
</html>
